name: academy_manager
description: Academy Manager Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_core_web: ^2.10.0
  firebase_auth_web: ^5.8.12
  cloud_firestore_web: ^3.8.10
  
  # Auth
  google_sign_in: ^6.1.6
  google_sign_in_web: ^0.12.3
  
  # State Management
  provider: ^6.1.1
  
  # UI
  cupertino_icons: ^1.0.2
  intl: ^0.19.0
  fl_chart: ^0.65.0

  excel: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/